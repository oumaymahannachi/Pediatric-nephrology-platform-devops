package tn.pedialink.messaging.client;

// TODO: Enable when Feign Client is configured
// import org.springframework.cloud.openfeign.FeignClient;
// import org.springframework.web.bind.annotation.GetMapping;
// import org.springframework.web.bind.annotation.PathVariable;
// import tn.pedialink.messaging.dto.ChildDto;

// @FeignClient(name = "treatment-monitoring-service", path = "/api/parent/children")
public interface ChildClient {
    
    // @GetMapping("/{id}")
    // ChildDto getChildById(@PathVariable String id);
}
